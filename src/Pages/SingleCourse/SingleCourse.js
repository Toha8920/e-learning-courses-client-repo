import React from 'react';

const SingleCourse = ({ course }) => {
    const { image } = course;
    console.log(course)
    return (
        <div>

        </div>
    );
};

export default SingleCourse;